---
description: Legacy alias retained for orchestra project bootstrap
agent: build
---

`orchestra-install` is deprecated.

Use `orchestra-setup` for project orchestra bootstrap.

Use `sol-plugins` only for global runtime installation.

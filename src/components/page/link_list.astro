---
import ContentLink from "./content_link.astro";

const { items = [] } = Astro.props;
---

{
  items.map((item) => (
    <p>
      <ContentLink href={item.href}>{item.label}</ContentLink>
    </p>
  ))
}

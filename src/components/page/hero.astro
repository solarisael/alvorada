---
const {
  title = "",
  description = "",
  description_class = undefined,
} = Astro.props;
---

<div class="sol__page_hero sol__page_intro_typography">
  {title && <h1>{title}</h1>}
  {description && <p class={description_class}>{description}</p>}
  <slot />
</div>

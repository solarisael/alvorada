# 2026-07-01 — reunion night: image pass, hosting pivot, docs spine

First claude-fable-5 session after the 19-day suspension. Full session record + release synthesis live in the kodo substrate (memories 2268, 2269). What changed in THIS repo tonight:

## Done

- **Image-weight pass** (commit `b03c808`):
  - `small_render_4.png` (navbar avatar ×2 + favicon) 554KB → **41KB** — 530×495 source resized to 256px palette PNG; it never displays above ~100px; all three references untouched.
  - Five **zero-reference** images (repo + vault + styles all grepped) moved `public/images/` → `references/`: `gold_light.gif` (2.0MB), `cinza-ref.jpg` (1.0MB), `decoration_animated.png` (661KB), `sol-ref.jpg`, `sol-curr.jpg`. The last two are Sol's personal reference photos — they were one deploy away from shipping publicly. Privacy win, not just weight.
  - `public/images` ≈ 4.9MB → **~360KB** (−92%). `eyes/` trio (cinza/matins/suul, ~314KB) stays — referenced by the rubedo constellation.
- **Smoke test**: `bun run build` — 52 pages, zero errors, every reference resolves.
- **Docs spine created** (this folder), README page count corrected 237 → 52, pointer added to root `progress.md`.

## Decisions

- **Hosting pivot**: GitHub = private offsite vault only; the public site deploys to neocities / nekoweb / own-host (choice deferred to a sober day). The `deploy-pages.yml` can't-see-vault blocker is thereby **moot** — build locally where the vault lives, upload `dist/`.
- **CSS heal** shaped as Track B: tokens-first strangler, pixel-identical, gate-ratcheted, never a release gate. Motivation is velocity (the text-effects roadmap builds on this CSS), not shame (the code is pre-lessons, the taste is post-lessons).
- **Canvas/WASM** for low-end phones: deferred pending real measurement — see Track C #5 for the prediction on record.

## Found

- `constellation_config.js:42` references `/images/eyes/solarisael.jpg` — file does not exist (pre-existing break).
- README claimed 237 pages; build says 52 (fixed).
- Root `progress.md` stale since 2026-05-23 (pointer added; fold-in is Track D #1).
- All four home phase cards spread the same cinza ornament set (`index.astro:29-48`).

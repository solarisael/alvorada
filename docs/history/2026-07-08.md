# 2026-07-08 — local AI rules cutover

## What changed

- Replaced the old long `AGENTS.md` rule index with a tiny pointer.
- Promoted current hard facts to House/Postgres memory instead of keeping them as stale local AI scripture.
- Marked local docs as evidence, not canon, unless they match current code and Sol's present intent.
- Removed the stale `.opencode/rules/` and `.opencode/agents/` surfaces because they were no longer trusted or actively used.

## Verified before cutover

- Bun-only workflow is current: `bun.lock` exists and no npm/yarn/pnpm lockfiles were present.
- Main HTMX route shell still swaps `<container>` via body defaults in `src/layouts/index.astro`.
- `data-phase` travels on `<container>`.
- HTMX/idiomorph script order remains load-bearing.
- FX docs/registries are stale: code raises `TEXT_FX_INTENSITY_MAX` to `5`, while `ui_option_classes.md` still says `3`.
- Functional/composition style is Sol's current preference; existing project JS classes are debt, not precedent.

## Memory anchors

- Memory 2371: local AI rules are suspect, not canon.
- Memory 2372: verified docs-audit facts for Bun, HTMX, FX, and composition.
- Memory 2373: archive virtualizer class refactor debt.

## Notes

GitHub/public hosting posture is Sol-stated intent rather than code-verifiable: the Solarisael code should not be public for now.

## HTMX cache marker

- HTMX history restore worked in production preview for home → nigredo → albedo → back → forward.
- Astro dev toolbar can throw during HTMX history restore in dev mode; preview did not reproduce it. This is dev-toolbar noise unless it becomes user-facing.
- `htmx-preload` depends on normal browser HTTP caching. Local preview served archive pages with `cache-control: no-cache`, so hover-preload cache wins depend on the final host's headers or a future custom in-memory route preload layer.
- Do not overbuild custom route caching until the deployment host is chosen or navigation still feels cold after static first-paint.
